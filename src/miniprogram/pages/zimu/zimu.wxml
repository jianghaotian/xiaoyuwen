<view class="topbar">
  <text>字母表</text>
  <text class="small">23个</text>
</view>
<view class="zimu">
  <view wx:for="{{list}}" wx:key="index" bindtap="playAudio" data-id="{{item}}" class="zimu_block">
    <text>
      {{item}}
    </text>
  </view>
</view>
